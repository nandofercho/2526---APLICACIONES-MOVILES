meta {
  name: /auth/change-password
  type: http
  seq: 4
}

post {
  url: {{url}}/auth/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "password_actual": "admin",
    "password_nuevo": "admin123"
  }
}
